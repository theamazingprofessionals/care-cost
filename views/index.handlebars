Handlebars.registerHelper('states', function(stateNames, options) {
  var out = "<ol>";

  for(var i=0, l=states.length; i<l; i++) {
    out = out + "<li>" + options.fn(items[i]) + "</li>";
  }

  return out + "</ol>";
});